import { UploadHandlerDirective } from './upload-handler.directive';

describe('UploadHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
